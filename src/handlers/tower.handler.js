export const purchaseTowerHandler = (userId) => {
    return null; 
};

export const upgradeTowerHandler = (userId) => {
    return null; 
};

export const sellTowerHandler = (userId) => { 
    return null;
};